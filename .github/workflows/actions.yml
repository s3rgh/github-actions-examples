name: Actions Workflow

on: [ push ]

jobs:
  run-github-actions:
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, linux ]
    runs-on: ${{ matrix.os }}
    steps:
      - name: list files
        run: |
          pwd
          ls -a
      - name: Set up working directory
        uses: actions/checkout@v3
      - name: list files
        run: |
          pwd
          ls
  run-github-actions2:
    runs-on: ${{ matrix.os }}
    steps:
      - name: list files
        run: |
          pwd
          ls -a
      - name: Set up working directory
        uses: actions/checkout@v3
      - name: list files
        run: |
          pwd
          ls